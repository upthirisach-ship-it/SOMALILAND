module.exports = {
    // Bot settings
    botName: "WhatsApp Bot",
    ownerName: "upthirisack",

    // Owner number (important for commands like owner-only)
    ownerNumber: "252672181226", // change to your number with country code

    // Prefix for commands (optional if you expand bot later)
    prefix: "",

    // Session folder name (Baileys auth data)
    sessionName: "auth_info",

    // Bot status message
    status: "Bot is running ✔",

    // Auto read messages
    autoRead: true,

    // Auto typing indicator
    autoTyping: true
};
